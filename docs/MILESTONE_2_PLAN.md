# Milestone 2 — compare LEFT and INNER JOIN

Use the same customers/orders fixture and retained compileTableJoin/JoinRenderer. Add a seven-step INNER trace aligned with LEFT so switching type preserves the explanatory step. Matching pair IDs remain identical; Bob's NULL-extended row disappears for INNER. Pause playback on mode changes without resetting the step.

Show a native radio choice, SQL clause and explicit output/customer counts. Preserve source rows and the renderer host across switches. No new upstream import, renderer, execution runtime or product area.

Protect final cardinality, unchanged matched IDs, unmatched behavior and aligned semantic references with unit tests. Browser coverage switches both directions at Bob and final output, verifies DOM identity, keyboard, reduced motion, phone overflow and Axe. Run the existing independent release gate before completion.
