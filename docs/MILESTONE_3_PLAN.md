# Duplicate-key join proof

Add a second Customers record with key C1 to the existing SQL comparison. Keep unique row IDs distinct from the repeated join key. Reuse compileTableJoin, JoinRenderer and the seven aligned steps. Two C1 customer records times two C1 orders produce four pairs, then Bob/Chloé bring totals to six LEFT or five INNER rows.

Add a keyboard-accessible dataset choice and distinguish customer records from distinct customers. Preserve the current step and pause playback on dataset switches. Keep diagram bounds fixed across modes and large enough for six rows. Test pair completeness, stable identities, all frames, responsive screenshots and the full release gate. No new upstream imports.
